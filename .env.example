# Admin credentials (optional, used for initial setup when no users exist)
# ADMIN_USERNAME defaults to 'admin' if empty
# ADMIN_PASSWORD is auto-generated if empty

# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your-secure-password

# Content-Security-Policy frame-ancestors directive (optional)
# Default when empty: frame-ancestors * (allow embedding from any origin)

# FRAME_ANCESTORS=*
